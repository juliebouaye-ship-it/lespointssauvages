{
  "name": "lespointsrebelles-site",
  "private": true,
  "type": "module",
  "scripts": {
    "logo:knockout": "node scripts/logo-knockout-white.mjs",
    "images:optimize": "node scripts/optimize-images.mjs",
    "box-photos:prep": "node scripts/prep-box-photos.mjs",
    "dev": "npx -y serve@14 . -l 5173 -n",
    "deploy:preview": "npx -y netlify-cli deploy --no-build --dir . --alias preview",
    "deploy:prod": "npx -y netlify-cli deploy --no-build --dir . --prod"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "nodemailer": "^8.0.7"
  }
}
